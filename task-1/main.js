const prompt = require('prompt');
prompt.start();


prompt.get('input', function (err, result) {
      
let count = 0
while (arr.length > 0) {
    count++
    let x = arr[0]
    arr.unshift()
    while (arr.includes(x)) {
        index = arr.indexOf(x)
        arr.splice(index, 1)
    }
}
console.log(count);

  
});
